{
  "name": "sleep-token-3d-chess",
  "private": true,
  "version": "1.0.0",
  "description": "Sleep Token 3D Chess — atmospheric multiplayer chess hosted on Netlify",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && netlify deploy --prod --dir=dist --no-build"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "peerjs": "^1.5.5",
    "three": "^0.185.1"
  }
}
